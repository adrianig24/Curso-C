

	float #include <stdio.h>
size_t getsize (float *apr)
int main(){array[20];
	printf("el numero de bytes es: \n", sizeof(array));
	return 0;
}

size_t getsize(float *apr)
{
return sizeof(ptr);
}